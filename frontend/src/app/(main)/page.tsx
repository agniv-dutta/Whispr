'use client';

import ChatPane from '@/components/ChatPane';

/**
 * Home page – shown in the main content area when no conversation is open.
 * The conversation list and search bar are rendered by the layout.
 */
export default function ChatListPage() {
  return <ChatPane />;
}
